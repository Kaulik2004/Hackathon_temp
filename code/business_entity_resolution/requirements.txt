# Core pipeline dependencies -- required for every run (local, no-embeddings
# fast path, and AWS mode). Versions pinned to what was installed and tested.
pandas==2.3.0
numpy==2.2.6
scikit-learn==1.7.0
scipy==1.16.0
pyarrow==23.0.1
boto3==1.42.80
rapidfuzz==3.10.1
lightgbm==4.5.0
xgboost==2.1.3

# Optional -- only required when running with --use-embeddings (or on a
# machine where a GPU is auto-detected). Skip these three for a fast,
# CPU-only, no-download plumbing/validation run: the default blocking path
# (exact keys + TF-IDF sparse matmul) works standalone without them, and
# pipeline.py/blocking.py only import these modules lazily, inside the
# --use-embeddings code path.
sentence-transformers==3.3.1
torch==2.5.1
faiss-cpu==1.9.0
